import { baseTheme } from '@chakra-ui/react';

export const global = {
    styles: {
        global: {
            'html, body': {
                bg: 'rgb(33, 0, 75)',
                color: 'white',
                overflow: 'hidden',
            },
        },
    },
};
