import React from "react";

type ConnectionListProps = {
    children?: React.ReactNode
}

const ConnectionList: React.FC<ConnectionListProps> = ({ children }) => {
    return <div></div>
}

export default ConnectionList
